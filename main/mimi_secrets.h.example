/*
 * MimiClaw Build-time Secrets
 *
 * This is the ONLY way to configure MimiClaw.
 * Copy this file to mimi_secrets.h and fill in your values.
 * After any change, rebuild: idf.py fullclean && idf.py build
 *
 *   cp mimi_secrets.h.example mimi_secrets.h
 *
 * Supported LLM Providers:
 *   - "anthropic" : Anthropic Claude API (default)
 *   - "openai"    : OpenAI GPT API
 *   - "kimi"      : Moonshot AI (Kimi)
 *   - "deepseek"  : DeepSeek
 *   - "glm"       : Zhipu AI (GLM)
 *   - "mimo"      : MiniMax (Mimo)
 *   - "custom"    : Custom API URL (set MIMI_SECRET_API_URL)
 */

#pragma once

/* WiFi */
#define MIMI_SECRET_WIFI_SSID       ""
#define MIMI_SECRET_WIFI_PASS       ""

/* Telegram Bot */
#define MIMI_SECRET_TG_TOKEN        ""

/* Feishu Bot */
#define MIMI_SECRET_FEISHU_APP_ID   ""
#define MIMI_SECRET_FEISHU_APP_SECRET ""

/* LLM Configuration
 *
 * Supported providers: anthropic, openai, kimi, deepseek, glm, mimo
 * For "custom" provider, also set MIMI_SECRET_API_URL below
 */
#define MIMI_SECRET_API_KEY         ""
#define MIMI_SECRET_MODEL           ""
#define MIMI_SECRET_MODEL_PROVIDER  "anthropic"

/* Custom API URL (for provider = "custom")
 * Example: https://api.example.com/v1/chat/completions
 * You can also use CLI command: set_api_url <url> && set_model_provider custom
 */
#define MIMI_SECRET_API_URL         ""

/* HTTP Proxy (leave empty or set both) */
#define MIMI_SECRET_PROXY_HOST      ""
#define MIMI_SECRET_PROXY_PORT      ""
#define MIMI_SECRET_PROXY_TYPE      ""   /* "http" or "socks5" */

/* Brave Search API (for web_search tool) */
#define MIMI_SECRET_SEARCH_KEY      ""
